<%= form_for(@intake_amount) do |f| %>
  <% if @intake_amount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@intake_amount.errors.count, "error") %> prohibited this intake_amount from being saved:</h2>

      <ul>
      <% @intake_amount.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :intake_amount %><br>
    <%= f.text_field :intake_amount %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
